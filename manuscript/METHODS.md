# Methods

This document is a template until real run manifests are ingested. Commands,
versions, hashes, counts, and thresholds will be updated automatically from
successful genuine runs. No performance values or dataset counts are currently
asserted.

The preregistered hypotheses and splits are in
`docs/EXPERIMENT_PROTOCOL.md` and
`experiments/preregistered/split_protocol.yaml`.

