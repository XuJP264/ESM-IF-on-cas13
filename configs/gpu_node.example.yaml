project:
  root: /scratch/USER/ESM-IF
paths:
  data: ${project.root}/data
  models: ${project.root}/models
  third_party: ${project.root}/third_party
  results: ${project.root}/results
  reports: ${project.root}/reports
  artifacts: ${project.root}/artifacts
runtime:
  device: cuda
  offline_inference: true
  seed: 20260731
  workers: 8
  batch_size: 1
  fail_fast_on_leakage: true

