# Environment locks

Exact explicit package lists, `conda list --explicit`, `conda list`, and
`pip freeze` snapshots are written here only after each environment is created
and its smoke tests pass. A specification file is not reported as a lock.

