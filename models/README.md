# Local model assets

Model checkpoints are local, ignored, independently licensed assets. Fetch them
explicitly with `make fetch-models`; inference never silently downloads them.
`manifest.yaml` records verified hashes, sizes, licenses, test status, and
environment after genuine loading.

